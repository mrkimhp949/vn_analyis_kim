"\"\"\"PhoBERT fine-tuning utilities for Vietnamese financial sentiment.\"\"\"\n+import logging\n+import os\n+import warnings\n+from dataclasses import dataclass\n+from typing import Dict, List, Optional\n+\n+import numpy as np\n+import pandas as pd\n+from sklearn.metrics import accuracy_score, f1_score, precision_score, recall_score\n+\n+# Suppress FutureWarning from transformers\n+warnings.filterwarnings('ignore', category=FutureWarning, module='transformers')\n+\n+logger = logging.getLogger(__name__)\n+\n+\n+@dataclass\n+class FineTuneConfig:\n+    data_path: str\n+    text_column: str = \"text\"\n+    label_column: str = \"label\"\n+    validation_split: float = 0.1\n+    model_name: str = \"vinai/phobert-base-v2\"\n+    output_dir: str = \"models/phobert_vi_financial\"\n+    num_epochs: int = 3\n+    batch_size: int = 16\n+    learning_rate: float = 2e-5\n+    warmup_ratio: float = 0.1\n+    weight_decay: float = 0.01\n+    max_length: int = 256\n+\n+\n+class PhoBERTFineTuner:\n+    def __init__(self, config: FineTuneConfig):\n+        self.config = config\n+        os.makedirs(self.config.output_dir, exist_ok=True)\n+\n+    def _load_dataset(self):\n+        try:\n+            from datasets import load_dataset\n+        except ImportError as exc:  # pragma: no cover - optional dependency\n+            raise RuntimeError(\n+                \"datasets library chưa được cài đặt. Vui lòng pip install datasets\"\n+            ) from exc\n+\n+        data_path = self.config.data_path\n+        ext = os.path.splitext(data_path)[1].lower()\n+        if ext in {\".json\", \".jsonl\"}:\n+            dataset = load_dataset(\"json\", data_files={\"train\": data_path})\n+        elif ext in {\".csv\", \".tsv\"}:\n+            dataset = load_dataset(\"csv\", data_files={\"train\": data_path}, delimiter=\",\" if ext == \".csv\" else \"\\t\")\n+        else:\n+            raise ValueError(f\"Định dạng dữ liệu không hỗ trợ: {ext}\")\n+\n+        # Train/validation split\n+        if \"validation\" not in dataset:\n+            dataset = dataset[\"train\"].train_test_split(\n+                test_size=self.config.validation_split,\n+                stratify_by_column=self.config.label_column,\n+                seed=42,\n+            )\n+        return dataset\n+\n+    def _prepare(self, dataset):\n+        from transformers import AutoTokenizer\n+\n+        tokenizer = AutoTokenizer.from_pretrained(self.config.model_name, use_fast=False)\n+\n+        label_column = self.config.label_column\n+        labels = sorted(set(dataset[\"train\"][label_column]))\n+        label2id = {label: idx for idx, label in enumerate(labels)}\n+        id2label = {idx: label for label, idx in label2id.items()}\n+\n+        def preprocess(examples):\n+            texts = examples[self.config.text_column]\n+            encodings = tokenizer(\n+                texts,\n+                padding=\"max_length\",\n+                truncation=True,\n+                max_length=self.config.max_length,\n+            )\n+            encodings[\"labels\"] = [label2id[label] for label in examples[label_column]]\n+            return encodings\n+\n+        tokenized = dataset.map(preprocess, batched=True)\n+        return tokenized, tokenizer, label2id, id2label\n+\n+    def train(self) -> Dict[str, float]:\n+        from transformers import (  # Lazy import to avoid heavy deps at module import time\n+            AutoModelForSequenceClassification,\n+            DataCollatorWithPadding,\n+            Trainer,\n+            TrainingArguments,\n+        )\n+\n+        dataset = self._load_dataset()\n+        tokenized, tokenizer, label2id, id2label = self._prepare(dataset)\n+\n+        model = AutoModelForSequenceClassification.from_pretrained(\n+            self.config.model_name,\n+            num_labels=len(label2id),\n+            id2label=id2label,\n+            label2id=label2id,\n+        )\n+\n+        data_collator = DataCollatorWithPadding(tokenizer=tokenizer)\n+\n+        training_args = TrainingArguments(\n+            output_dir=self.config.output_dir,\n+            evaluation_strategy=\"epoch\",\n+            save_strategy=\"epoch\",\n+            learning_rate=self.config.learning_rate,\n+            per_device_train_batch_size=self.config.batch_size,\n+            per_device_eval_batch_size=self.config.batch_size,\n+            num_train_epochs=self.config.num_epochs,\n+            warmup_ratio=self.config.warmup_ratio,\n+            weight_decay=self.config.weight_decay,\n+            load_best_model_at_end=True,\n+            metric_for_best_model=\"f1\",\n+            greater_is_better=True,\n+            logging_steps=50,\n+            report_to=[],\n+        )\n+\n+        def compute_metrics(pred):\n+            preds = np.argmax(pred.predictions, axis=-1)\n+            labels = pred.label_ids\n+            return {\n+                \"accuracy\": accuracy_score(labels, preds),\n+                \"precision\": precision_score(labels, preds, average=\"weighted\", zero_division=0),\n+                \"recall\": recall_score(labels, preds, average=\"weighted\", zero_division=0),\n+                \"f1\": f1_score(labels, preds, average=\"weighted\", zero_division=0),\n+            }\n+\n+        trainer = Trainer(\n+            model=model,\n+            args=training_args,\n+            train_dataset=tokenized[\"train\"],\n+            eval_dataset=tokenized[\"test\"],\n+            tokenizer=tokenizer,\n+            data_collator=data_collator,\n+            compute_metrics=compute_metrics,\n+        )\n+\n+        logger.info(\"Bắt đầu fine-tune PhoBERT trên dữ liệu tài chính Việt...\")\n+        trainer.train()\n+        trainer.save_model(self.config.output_dir)\n+        tokenizer.save_pretrained(self.config.output_dir)\n+\n+        eval_metrics = trainer.evaluate()\n+        logger.info(\"Fine-tune hoàn tất. Eval metrics: %s\", eval_metrics)\n+        return eval_metrics\n+\n+\n+def fine_tune_phobert(config: FineTuneConfig) -> Dict[str, float]:\n+    \"\"\"Convenience wrapper cho việc fine-tune PhoBERT.\"\"\"\n+    tuner = PhoBERTFineTuner(config)\n+    return tuner.train()\n*** End Patch

