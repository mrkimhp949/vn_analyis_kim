# Trading Bot - Optimized for Python 3.11 & Render
python-dotenv==1.0.0  # Load .env file
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-telegram-bot==21.7
httpx==0.27.0
requests==2.31.0
matplotlib==3.8.2
ta==0.11.0
joblib==1.3.2
pytz==2024.1
openpyxl==3.1.2
python-multipart==0.0.6
transformers==4.35.2  # Optional: advanced sentiment
torch==2.0.1          # Optional: required by transformers
xgboost==2.0.3        # Optional: ensemble models & volatility forecasting
# TensorFlow: LSTM models support
# Note: If you have tensorflow-cpu installed, uninstall it first: pip uninstall tensorflow-cpu
# Then install: pip install tensorflow==2.15.0
# tensorflow==2.15.0    # Optional: LSTM models (install separately to avoid conflicts)
pyarrow==14.0.1       # Required: parquet file support for feature store
lightgbm==4.1.0       # Optional: stacking meta-model
shap==0.44.0          # Optional: feature importance analysis
hmmlearn==0.3.0       # Optional: Hidden Markov Model for market regime
datasets==2.16.1      # Optional: for fine-tuning PhoBERT
scipy==1.11.4         # Optional: HRP optimizer distance metrics
yfinance==0.2.36      # Market data from Yahoo Finance
prometheus-client==0.19.0  # Prometheus metrics export
websockets==12.0       # WebSocket client for real-time price feeds

# Development & Testing
pytest==7.4.3          # Unit testing framework
pytest-cov==4.1.0      # Test coverage reporting

# Code Quality Tools (Development only)
black==23.12.1         # Code formatter
isort==5.13.2          # Import sorter
flake8==7.0.0          # Linter
mypy==1.8.0            # Type checker
bandit==1.7.6          # Security scanner
pre-commit==3.6.0      # Pre-commit hooks
safety==3.0.1          # Dependency security checker (updated for packaging>=22.0 compatibility)